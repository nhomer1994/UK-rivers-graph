name: uk-river-pipeline
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - geopandas>=0.14.0  # Safely delivers underlying GDAL, PROJ, and GEOS binaries
  - shapely>=2.0.0
  - pip
  - pip:
      - -r requirements.txt
